# Local development — Vite proxies /api to your backend (see vite.config.ts)
VITE_API_BASE_URL=/api

# Production / direct API access (uncomment and set for builds without the dev proxy)
# VITE_API_BASE_URL=https://your-api-host.example.com/api
